spring:
   application:
      name: dm-spring-zuul
   cloud:
     config:
       uri: http://localhost:7900
       profile: dev
       label: springconfig
#config server 地址
#application name + profile版本(dev开发，pro生产，test测试)
#分支